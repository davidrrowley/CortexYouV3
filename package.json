{
  "name": "cortexyou",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/web",
    "apps/api"
  ],
  "scripts": {
    "build:web": "npm run build --workspace=apps/web",
    "build:api": "npm run build --workspace=apps/api",
    "build": "npm run build:api && npm run build:web"
  },
  "overrides": {
    "@azure/storage-blob": "12.26.0",
    "@azure/storage-blob@^12.23.0": "12.26.0"
  }
}
