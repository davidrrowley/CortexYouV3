# Web app environment variables
# Copy to .env.local for local development (never commit .env.local)

# API base URL – leave empty to use relative /api (correct for SWA and SWA CLI)
VITE_API_BASE_URL=
